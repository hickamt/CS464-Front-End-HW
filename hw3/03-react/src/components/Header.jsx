/* eslint-disable react/prop-types */
// Header setup for testing
function Header({title}) {
  return (
    <div className="header">
      <h1 className="got-title">{title}</h1>
    </div>
  );
}

export default Header;

